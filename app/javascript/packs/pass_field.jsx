import React from 'react'
import ReactDOM from 'react-dom'
import PasswordUpdate from 'PasswordUpdate'

var element = document.getElementById("password_component");
    ReactDOM.render(<PasswordUpdate />, element);